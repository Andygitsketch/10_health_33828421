<DOCTYPE html> 
   <html>
    <body>
        <ul>
         <% for (let i =0;i <availableMeasurements.length; i++) {%>
            
            <% if (availableMeasurements[i].height!=undefined) %>
            <li><%= availableMeasurements[i].height %>  </li>
            <% } %>
        </ul>
    
    </body>
</html>